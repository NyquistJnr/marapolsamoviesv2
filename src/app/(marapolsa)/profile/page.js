import MainProfile from "@/components/custom-components/profile-component/MainProfile";

export const metadata = {
  title: "Profile",
};

const ProfilePage = () => {
  return <MainProfile />;
};

export default ProfilePage;
